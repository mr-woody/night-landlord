# 可执行目标-M5：等距 2.5D 立体视觉升级（CoC 基准，v1.0）

> 定位：将全案视觉从「2D 静态平面图」升级为「**等距剖面 2.5D 动态视觉**」——参考部落冲突（CoC）的
> 立体性设计：等距场景/体积光影/接地投影/实体动画（怪物移动·人物打斗）/物品特效/动态光照/天气氛围。
> 来源：2026-09-01 用户方向决议——"完整实行等距剖面 2.5D 视觉，不是 2D 静态图效果；怪物移动、
> 人物打斗、物品特效、光线、天气参考 CoC 的 UI 效果与立体性设计"。
> 方法：文件级冲突审计 → 逐点修复 → 校验闭环 → 反复迭代直至验收全绿。
> 上位/平位文档：《战斗演出与天气系统设计》v1.1（演出层上位，本档不重复其契约）｜《2D美术资产规范》v2.1（资产层细则）｜《UI-UX设计规范》§五.1｜《素材提示词模板》§0

---

## 0. 现状盘点（审计结论：引擎表现层已就位 70%，资产层缺口 100%）

**已就位（M3.0–M3.3 交付，本档不重做）**：
| 能力 | 载体 | 状态 |
|---|---|---|
| 等距布局（isoToScreen/ISO_TILE/楼层体积） | `whitebox/layout.ts`、L2 小区地图 | ✅ M3.0 F6 |
| 天气表现层（光照 overlay+雨/雪/雾/尘粒子+HUD 角标） | `whitebox/renderer.ts` drawWeatherLayer + `config/weather.json` | ✅ M3.1 F4 |
| 独栋小屋群落（30 栋 6 级进化+窗光/烟囱） | renderer 小屋绘制函数 | ✅ M3.1 F5 |
| 夜战类 3D 演出（三车道深度/影子/状态机/技能 VFX/崩解粒子） | `whitebox/battle.ts` battleTimeline + §10.1–10.5 规范 | ✅ M3.1 F6 |
| 房屋升级交互（EffectOp） | 小屋面板 | ✅ M3.1 F7 |
| 程序化质感基线（面板立体感/矢量图标/真字体） | renderer（M2.6） | ✅ |

**核心缺口**：以上全部由**程序化矢量绘制**承载（白盒定位"零素材依赖"）——2.5D 立体感天花板被
占位级美术锁死。CoC 式立体性（体积光影的 sprite、接地投影的实体、分层部件动画、粒子贴图、
光效贴图）**一张真资产都还没有**。本档任务=把资产层造出来并接入既有表现层。

## 1. 目标视觉基准（CoC 特征 → 本项目映射）

| CoC 特征 | 本项目落点 | 验收口径 |
|---|---|---|
| 等距场景（固定 45°俯角/方位角，全场景统一） | 主楼剖面/小屋×30/公共建筑/室内视图 | 同屏多实体透视消失点一致，截图比对 |
| 实体体积光影（sprite 内置左上光+暗部色阶） | 房屋/怪物/住户/物品 全部 sprite | 暗部/亮部两阶以上，光源方向一致 |
| 接地投影（椭圆影，高度=影分离） | 夜战全实体（§10.1 已规范） | 飞行种影偏移可辨 |
| 实体动画（idle 呼吸/行进 bob/攻击扑击/受击/死亡） | 怪物 5 态、住户 4 态（§10.2 状态机已有） | 每实体 ≥3 态可辨，节拍对齐 §2.5 |
| 物品特效（落地光圈/光柱/弹跳） | 空投/物资拾取/技能 VFX（§10.3 表已有） | 差异化可辨 |
| 动态光照（窗光/月光/血月红光/技能光） | 昼夜窗光+夜战月光向+天气 overlay（既有）+ 新增光效贴图（光柱/光圈/辉光） | 血月夜全场红移可辨 |
| 天气氛围 | 粒子层+光照 overlay（既有）+ 天气 sprite（雪顶积尘/湿地反光） | 6 天气截图各可辨 |

## 2. 技术路线（K-V1 决策）

| 路线 | 评估 |
|---|---|
| **A. 部件化 2.5D 混合**：AI 生成等距视角 sprite（分层部件：本体/附件/眼爪发光件）+ 程序化骨骼变换动画（bob/lean/扑击=部件 rotate+translate，白盒既有手法）+ 粒子贴图 + 光效贴图 + 影子椭圆 | ✅ **采纳**：复用既有 battleDirector/状态机/粒子层（只换绘制源）；AI 生图管线直接产能；主包增量可控（@2x sprite + 图集）；确定性回放契约不破坏 |
| B. 实时 3D（Meshy/Tripo 产模 + Cocos 3D 等距相机） | 备选评估（P3 后）：真立体但包体/低端机性能/管线成本三重冲击，主包 4MB 红线下引擎 3D 模块+模型资产不可承受；与 AI 生图闭环不兼容 |
| C. 全 Spine 骨骼动画 | P3 评估项：表现力上限高，但 spine 运行时包体+部件拆分绘制成本大；A 路线的部件变换若不够再升 C（部件资产可复用） |

**路线 A 分层契约（继承 §2.1 确定性红线）**：资产=静态 sprite+部件图；动=程序化变换时间线
（battleDirector 派生，可由 session+seed 复现）；光=overlay/光效贴图乘算；粒子=tokens 派生参数。

## 3. 视角规格（资产层新增硬规范，全管线统一）

| 资产类 | 视角 | 关键词 |
|---|---|---|
| 主楼/小屋/公共建筑/天气场景 | **等距** | 等距 2.5D 视角，俯角约 30°，方位角 45°（左上前），统一消失点，带接地投影 |
| 怪物/住户（战斗实体） | **3/4 侧面** | 面朝右（行进方向→小屋在车道右端），带接地椭圆影 |
| 住户头像/图鉴立绘 | 正面半身 | 平视（既有不变） |
| 图标/UI/VFX 贴图 | 正视 | 居中单体 |
| 粒子贴图（烟/尘/光斑/火花） | 正视软边 | 允许渐变透明（豁免色板硬门，单列 particle 门） |

## 4. 文件级冲突清单与处置（审计轮 1，2026-09-01）

| # | 文件 | 现状（冲突点） | 目标 | 阶段 |
|---|---|---|---|---|
| C1 | `docs/2D美术资产规范.md` §头/§一 | "全案锁定 2D（…无透视、无 3D 建模）""渲染 2D 正交剖面，无透视" | 升 v2.1：基调改「等距 2.5D 混合（路线 A）」，渲染行改「等距 2.5D：场景等距/实体 3/4 侧/头像图标正视（§视角规格见 M5 目标档 §3）」 | P0 ✅本轮 |
| C2 | `docs/素材提示词模板.md` §0 | 统一后缀含"2D 正交无透视" | §0 拆为「分视角后缀表」（等距/3-4 侧/正视三档） | P0 ✅本轮 |
| C3 | `docs/UI-UX设计规范.md` §五.1-2 | 统一后缀同上 | 同步分视角后缀 | P0 ✅本轮 |
| C4 | `scripts/gen-ai-assets.mjs` | SUFFIX 写死"2D 正交无透视"；资产无视角字段 | per-asset `view` 字段+三档后缀拼装 | P0 ✅本轮 |
| C5 | `docs/素材提示词模板.md` §2/§3 | 房屋"等距"已有但无俯角/方位角/投影规范；怪物无朝向 | 按 §3 视角规格补关键词 | P0 ✅本轮 |
| C6 | 资产清单（gen-ai-assets 内置） | 缺动画集/部件/粒子贴图/光效贴图类目 | ✅ P2-1（495bc9e）：+monster_seeker_attack（§10.2 attack 帧）、粒子×4、光效×3（fx 软边门）；守卫战场立绘列入 P2-2 补产清单 |
| C7 | `whitebox/renderer.ts` | 全程序化矢量绘制，无 sprite 承载 | ✅ P3-1（7e2d04d）：SpriteStore+drawHouse/drawMonster sprite 层，缺资产回退程序化（零回归）；Creator/小游戏侧资产打包随 E6 上线批 |
| C8 | `whitebox` 动画 | bob/白闪/击退=整体变换 | ✅ P3-2（97c19a5）：pickMonsterSprite 纯函数+攻击相位切换 attack 帧+前倾变换（5 单测）；守卫保持程序化（字段化立绘待补产） |
| C9 | `scripts/check-assets.mjs` | 仅扫 anchors/ui 两目录、anchor_/ui_ 前缀、色板 ≥98% 硬门 | ✅ P2-1（495bc9e）：扫 ai/ 六目录+新前缀；AI 资产扩展色板 85% 双轨门；fx_ 软边门；全出血豁免 |
| C10 | 包体（`check-size.mjs` NFR-7） | 主包 1360KB/4MB | ✅ P3-4（bac3f67）：sprite 经 sync-sprites 入 whitebox/assets（分包/CDN 预算线），主包 1360KB 不变、门禁复绿 |
| C11 | `docs/永夜苟神-游戏完整设计方案.md` §5.2 | 装修皮肤三套按 2D 表述 | 皮肤规格挂 §3 视角规格（引用即可） | P1 |
| C12 | README/追溯矩阵 | M5 未登记 | 目标档入库后补登 | P0 ✅本轮随档 |
| C13 | 色板门（`check-assets` §十六①→管线复刻） | "逐像素吸附 theme 12 色板"——P1 冒烟实测：AI 头像对基础色板命中率仅 33.8%，全吸附毁掉皮肤/发色等中间色 | 决议（2026-09-01，实测定标）：新增 `config/asset-palette.json` 扩展资产色板（基础 12 色+资产中间色，v3 共 31 族=43 色），AI 资产门禁=扩展色板命中率 ≥85%；snapPalette 改容差内吸附；**挑选评分加入命中率权重（×50）**——迭代 2 实测物资雨存在"95.7% 合格候选被无命中率评分埋没"缺陷；扩列须修订该配置。基础 12 色对 UI/白盒资产的 ±10% 硬门不变 | P0 ✅ |
| C14 | 提示词工程（P1 实测新发现） | PALETTE_HINT 列 hex 色码→模型把它画成色卡图例+乱码文字（主楼剖面/物资雨中招，程序门禁测不出文字，靠人眼层抓获） | 决议：提示词禁列 hex 码（色板一致性由扩展色板门禁+锚点参考图保障）；NEG 增加"色卡、图例、标注、边框装饰"；与 hex 相关的两资产改写 override 后 1–2 轮内收敛 | P1 ✅ |
| C15 | 全出血场景资产（P1 实测新发现） | 主楼剖面/天气氛围=整幅含夜景天空的 key art，对它们走"抠底+透明底门"是单体资产规则误伤 | 决议：管线新增 fullBleed 通道（不抠底、豁免透明底门、覆盖率按满幅 85–100% 计量） | P1 ✅ |

## 5. 功能点序列（逐点：分析→实现→自检→验证→提交）

| 点 | 内容 | 验收门 |
|---|---|---|
| P0-1 | C1–C5、C12 规范/管线提示词对齐 | `gen-ai-assets --dry` 24/24 过门；check-* 全绿 |
| P1-1 | 等距锚点 6 张重定调（主楼剖面/房屋 Lv3 基准/怪物基准×2/住户/天气各一）+ **用户确认**（B1 环节） | 锚点视觉审：视角一致/光影方向一致/色板锚定；用户签字 |
| P2-1 | C6/C9：资产清单扩容+门禁分列 | dry-run 新清单全过；`check-assets` 扩展后全绿 |
| P2-2 | Seedream 真跑：锚点→房屋 6 级（参考链锁一致性）→怪物→粒子/光效贴图 | 每资产 8 候选→过门→ai-report 全 pass；视觉审抽检 ≥30% |
| P3-1 | C7：renderer sprite 层（回退机制） | headless 截图：有 sprite/回退两态各自正确；E2E 全绿 |
| P3-2 | C8：部件变换动画（对齐 §10.2） | 夜战全流程截图序列：扑击/受击/崩解逐帧可辨 |
| P3-3 | 光效贴图叠加（窗光辉光/空投光柱/血月全屏红移强化） | 血月夜/雨夜截图比对 P2 基线 |
| P3-4 | C10 包体复测 | `check-size` 复绿（主包增量 ≤800KB） |
| P4-1 | 全量验收：E2E + 6 天气×昼夜截图矩阵 + 追溯矩阵/上线清单核销 | 全部门禁绿 + 用户终审 |

## 6. 迭代协议（本档执行方式）

每轮 = 文件级审计 → 修复 → `npm run gen:assets:ai -- --dry` + `check:assets` + `check:theme` +
`check:config` + `check:size`（P3 起）+ E2E → 台账留证 → 下一轮。冲突清单 §4 随轮滚动增补，
处置状态实时更新；全部门禁绿且用户终审通过后本档关闭、台账核销。

### P1 执行记录（2026-09-01，Seedream 真跑）

| 轮 | 动作 | 结果 |
|---|---|---|
| 0 冒烟 | worker×4 候选 | 连通/画质 ✓；连环暴露 C13（色板门毁图）/尺寸下限/JPEG/rembg CLI 冲突，逐一修复 |
| 1 | 锚点 6×8 候选 | 5/6 过；人眼层抓获 C14（hex 色卡图例）+ C15（全出血误抠） |
| 2 | C14/C15 修复后重跑 5 张 | 3/5 过（building 1 轮收敛）；worker/rain 卡色板命中率→实锤挑选评分缺命中率权重 |
| 3 | 评分+色板 v3+override 修正，重跑 2 张 | **6/6 全过**（worker 1 轮/rain 2 轮） |
| 记账 | 累计生成 104 张（含全部重试） | ≈¥26（单价按 ¥0.25/张估） |

### P2–P3 执行记录（2026-09-02，逐功能点：实施→自检→验证→提交）

| FP | 内容 | 结果 |
|---|---|---|
| P1 收尾 | be70d70 | 锚点+管线+规范入库（B1 经用户指令确认通过） |
| P2-1 | 495bc9e | 清单扩至 32+fx 软边门+check-assets 双轨门；dry 32/32 |
| P2-2 | **⛔ 阻塞** | 方舟账户欠费（ARK 403 AccountOverdueError，26 资产×8 候选全拒）——已加快速失败守卫（7b83cae，退出码 3）；**充值后重跑 `npm run gen:assets:ai -- --only houses,monsters,weather,vfx,fx` 即续**（预估 ≤¥60） |
| P3-1 | 7e2d04d | SpriteStore+sprite 层（回退零回归）；`?spritedbg=1` 画廊实证锚点 sprite 真实渲染 |
| P3-2 | 97c19a5 | 攻击姿态切换纯函数+前倾（+5 单测） |
| P3-3 | 8043ecc | 血月红移 vignette+空投光柱（additive，回退渐变）截图实证 |
| P3-4 | bac3f67 | sync-sprites 运行时打包链+清单驱动加载（零 404）；E2E 20/20 复绿；主包 1360KB 不变 |

**M5 当前完成度**：规范/管线/门禁/引擎接入层 100%；AI 资产量=锚点 6/32（P2-2 批量待账户充值，
管线与引擎两侧已就绪，补产重跑+重建即自动接入）。

### P2-2 续跑收官（2026-09-02，Seedream 5.0 pro）

| 项 | 结果 |
|---|---|
| 模型 | `doubao-seedream-5-0-pro-260628`（API 实测定位；单张 960² 实测延迟 ~41s） |
| 提速（C17） | 诊断=候选串行 await 为时延主因；改并发×4+rembg 异步+--skip-existing 断点续跑，单轮 5.5min→~95s |
| 管线再修 | ensurePng 异步竞态（致一批下载报废）、挑选过门优先 +1000（杜绝合格候选埋没）、manifest 磁盘全量扫描、--repick 零成本回收 |
| 产出 | **终图 31/32**：房屋 6/6（1 轮全过）、怪物 5/5（attack 帧含）、天气 6/6、vfx 空投、粒子光效 7——其中 11 枚经 --repick 零 API 成本回收 |
| 缓产 | vfx_shield（能量青色族已入色板 v3.1；程序化半球罩兜底在位，随下次批量补产） |
| 终验 | check-assets 48 资产全过 + theme/config/size 全绿 + 测试 93 + E2E v3 20/20 + spritedbg 画廊 16/16 全载 + L2 村落 AI sprite 实机渲染 |
| 记账 | P2-2 续跑新增 ~370 张（含故障批次损耗——串行/竞态两轮浪费已由守卫与并发修复根治） |

**M5 状态：目标达成（31/32，1 枚缓产有兜底）**。遗留打磨项：天气氛围图浅灰底→整幅夜空、
野猪/夜王/兔子朝向渲染翻转、守卫战场立绘资产、Creator/小游戏侧 sprite 打包（随上线批 E6）。

产物：`docs/assets/ai/anchors/`（6 张终图，四门+人眼双审通过）＋`ai-report.json`（逐资产轮次/病因/评分留证）。
**B1 关口：等用户确认 6 张锚点定调** → P2（清单扩容+房屋/怪物/天气批量+粒子/光效贴图）。
