# 可执行目标：M2 竖切 Demo（对标 Nodus 任务书模板 v1.0）

> 前置状态：M1 白盒可玩版已完成并合入 main（PR #1，门①–⑤全过，证据见 docs/M1-证据台账.md）。本目标为 M2 开工指令，**只产出指令文本，粘贴为会话目标后执行**。
> 核心使命：① 闭环 M1 FINDING-1（住户升级线）；② D0–D7 完整体验（事件引擎条件触发版 + 特殊夜三机制）；③ Cocos 真实工程接入与白盒渲染。

---

## 一、边界与合同

在仓库 /Users/woody/WorkSpaces/github_work_workspace/woodys/games/night-landlord（GitHub: mr-woody/night-landlord）内，从 main 创建分支 m2/vertical-slice，实现 M2 竖切 Demo（里程碑定义见 docs/技术架构与模块规划 §八、docs/架构需求文档 §8）。

边界与合同：实现只依据本仓库 docs/ 已验收文档（同 M1 合同）+ docs/M1-证据台账.md（FINDING-1 为本阶段硬需求）。不修改 docs/ 已冻结结论（ADR D1–D16）；实现与文档冲突时停下走 ADR 修订，不得静默偏离。延续 M1 全部红线：不引入 @deepseek-ai/*、cordisjs/cordis（ADR-11）；不做远程代码加载（ADR-16）；状态变更仅经 applyEffects 单点（D4）；逻辑层禁用 Math.random/Math.pow/Date.now。保护 docs/ 提交历史。

用户侧前置（开工前一次性完成）：经 Cocos Dashboard 登录 Cocos 账号并安装 Cocos Creator 3.x（微信开发者工具 2.02.2608060 已装并登录）。

执行方式：同 M1——实现者承担实现，主会话负责依赖排序、契约检查（headless verify + 新增 β_sim 硬门）、语义评审、证据台账（docs/M2-证据台账.md）、提交与集成；每功能点独立提交并记录哈希。

## 二、M1 遗留闭环（本阶段最高优先）

- **FINDING-1**：白盒无住户升级线时 β_sim 达 60–99%（设计 27/42/58%），D21/D28 血月预算崩塌。M2 实现升级线后，verify 的 β_sim 断言从 NOTE 升级为 **FAIL 硬门（±5pp，四周期）**。
- **事件引擎覆盖**：event_lib 尚缺 choice 24/mission 10/ord 8 共 42 条；M1 Director 仅固定脚本版。

## 三、按依赖顺序的功能点

每点先跑变更范围 Unit-test，再跑 headless 契约，通过后独立提交并记录哈希：

1. **住户升级线（FINDING-1 闭环）**：新增 UPGRADE_TENANT 效果/命令（level+1，租金 ×G_U^(level−1)，成本=UPGRADE_BASE×1.18^(level−1) 金币，UPGRADE_BASE 进 constants 并配区间）；uniform/greedy 策略加入升级购买（发展预算优先级：防御 fReq → 升级 → 招募补位）；verify 的 β_sim 断言升级为 FAIL 门（±5pp×4 周期）——模拟收入须回落至设计曲线 ±10% 内。
2. **event_lib 补全 50 条 + Director 条件触发版**：按 M0 事件库文档 B/C/D 组结构化 42 条入 config/event_lib.json（p 合计=1、护栏、KILL 禁用断言随库升级为 50 条全量）；Director 增加权重×cooldown×prereq 抽取与保底池；移除固定脚本通道（A 组 8 条并入权重池，教学事件保留 triggerDay 特权）。
3. **特殊夜三机制运行时**：SILENT（隐藏预告信息，考验冗余布防）、MIGRATE（开战时重排目标房间）、BLOOD_MOON（J 跳变已有）接入 NightPlan 与 UI 提示层（白盒日志即可）；对应 monster.json usableNightMods 生效。
4. **恐慌全链路 + 岗位槽位**：破防→同层传播→医务室治疗（clinic 等级→治疗量）→公约效果位；守卫岗位槽（building_def slots）接入战力公式（岗位空缺=战力折扣）。
5. **Stabilizer L1 + 观测埋点**：财富指数/产出消耗比/恐慌总量度量打点（只记录不干预，L2 阈值留 constants）；DomainEvent → 遥测通道（headless 落 JSON 报告）。
6. **Cocos 真实工程接入 + 白盒渲染**：Cocos Creator 3.x 新建 2D 竖屏工程接入 sync 产物（ADR-9 方案 a）；白盒渲染：竖屏剖面占位 UI（房间格子/人口/金币/日次/夜战结果面板）；物资雨占位动画 + 性能预算冒烟（中端机帧率采样脚本）；微信构建回归（验收门③复跑）。
7. **M2 验收门整备**：D0–D7 七日全事件模拟跑通；事件触发覆盖率 ≥80% 断言；β_sim 硬门；全部证据入 docs/M2-证据台账.md 并推送。

## 四、整体验收门（全过才算 M2 完成）

① 住户升级线闭环：β_sim 四周期 ±5pp FAIL 门全绿；② D0–D7 体验完整：50 条事件库全量入库、条件触发覆盖率 ≥80%、特殊夜三机制可复现；③ Cocos 真实工程在微信开发者工具构建通过（门③复跑）+ 白盒渲染帧率预算达标；④ 事件频控合规（单事件出场 ≤3 次/30 天）；⑤ 全功能点独立提交 + M2 台账完整。

## 五、使用注意

1. 功能点 6 的 Creator 安装是用户侧前置；未安装前功能点 1–5 可全部完成（headless 侧不依赖引擎）。
2. M2 完成后 M3（买量素材 CTR + 留存测试）进入真实用户域，不在会话可执行范围内。
