# M2.5 证据台账（m2p5/ui-styling 分支）

> M2.5 UI 风格化执行证据账本：功能点 → 提交哈希 → 验证证据。契约源：docs/可执行目标-M2.5-UI风格化.md；
> 设计合同：docs/UI-UX设计规范.md v1.0（界面层）+ docs/2D美术资产规范.md v2.0（§八/§十五/§十六）。
> 边界遵守：全部为表现层实施（白盒渲染器/新增 UI 组件/脚本），formula/systems/director 零改动；
> sim.ts 仅为渲染投影做纯加法字段（eventCards，不入 finalHash，行为零变化证据见 §6）。

## 1. 功能点提交台账

| 功能点 | 内容 | 提交 | 证据 |
|---|---|---|---|
| P1 | theme.json 双端驱动 + check-theme CI 扫描 | `924f0ac` | theme.ts tokens 桥（色板/字号/间距/圆角/动效全取 config/theme.json，withAlpha 唯一透明度入口）；M2 版硬编码色（#7ec8ff/#9fd8ff 等）改由 success/text_secondary/alert_blood 承担（色彩角色对齐 §二）；check-theme.mjs：白盒源码+bundle+index.html 扫非 theme hex 即 FAIL + 裸 rgba(数字) FAIL + bundle 重编译同步守卫；whitebox 独立 tsconfig（strict）入 typecheck；单测 8 项（motion 表逐条比对/缓动参考值） |
| P2 | 主界面风格化（§3.1 线框 1:1）+ UI 状态机 | `fa132dc` | 画布升 750×1624 设计逻辑分辨率；HUD 48px（日次/血月周期 🌙n/4/⚙ 88px 热区）+资源栏（金=gold_primary、恐慌=panic 紫）+剖面楼栋 6F×5（占用亮格/家具剪影/1F 公共三件/6F 瞭望塔/破防房 alert_blood threat 脉冲）+事件卡入口（weight 高在前）+昨夜战报条（恐慌条）+dock 四键 88px；state.ts：四相单向循环/事件卡打断队列 ≤2 LIFO 恢复/夜战锁/选择即锁定；sim.ts 加法投影 eventCards；单测 +8 |
| P3 | 事件卡/夜战面板/收租结算 + 五曲线落地 + 四相交互 | `b07121d` | 事件卡模板（标题栏/正文 24px/选项按钮概率徽标+风险星级/翻面→结果（实际掷中效果摘要）→金币飞资源栏）；夜战面板（血月 threat 红闪×2+震屏/路血条三态/主动技 CD 环 88px/战况日志）；收租结算（物资雨 rain 500 easeOutBack 24 粒子→计数器 counter 800 easeOutCubic→逐户 stagger 60ms+fast 滑入）；DUSK 横幅（血月/静默「?」/迁移+布防确认）；昼夜 dissolve 800 linear；theme.json 补 counter/stagger tokens（§3.4 明确值，§二五条未动）；anim.ts 纯函数时间线 + 单测 8 项逐条比对；headless 截图四相冒烟 |
| P4 | 图鉴/商店/设置占位页 + 合规扫描入 CI | `7e614e8` | 三占位页（图鉴 3 列网格剪影🔒/商店礼包横滑+删除线价格锚点+双倍角标/设置列表+开关占位）；⚙ 改页面导航（setPage 非 DAY 禁航）；check-ui-compliance.mjs（验收门⑤）：交互热区 14 处 ≥88×88、font(数字px) 字面量 FAIL、motion('name') 引用合法性、body≥24/caption≥18；CI 增 check:ui |
| P5 | AI 资产管线试跑（占位级） | `eb34cba` | 零依赖 PNG 编解码+硬边光栅器（scripts/lib/png.mjs）；gen-assets.mjs 走完 §十五 四段流程（8 候选种子化变体→评分挑选→清理吸附→图集集成）；产出锚点 6 张（主楼剖面/住户头像×3/循声者/物资雨帧）+ UI 素材 10 个（§八：九宫格/按钮三态/血条/恐慌条/资源条/开关/头像框/红点）+ ui_atlas 图集；check-assets.mjs（§十六）：色板 ±10% 逐像素/72px 剪影×8/深色描边/透明底/命名规范，17 资产全过，入 CI |
| P6 | Creator 原生工程接入 + 门③复跑 | ✅（本节提交） | **引擎自行获取**：官方直链下载 Creator 3.8.8 mac 包（1127MB，download.cocos.com）→ /Applications，CLI 可启动且无登录门槛。工程 `apps/client-cocos/creator/`（2D 竖屏 750×1624）：① `scripts/sync-creator.mjs` 同步 14 文件（packages→shared、headless sim→shared-headless、whitebox 渲染层→whitebox-core+theme-data 生成、五表→shared-tables 生成）；② `WhiteboxMain.ts` 组件（RawCanvas2D 桥：离屏画布跑 runSimulation 回放+WhiteboxRenderer，动态纹理 ImageAsset/Texture2D/uploadData 每帧上传，触摸→hitTest→state 相位分发——渲染/状态/动效代码零重写）；③ 场景由 `scripts/gen-creator-scene.mjs` headless 生成（main.scene：Canvas+UITransform+Widget+cc.Canvas+WhiteboxMain+正交 Camera，Creator 3.8 序列化格式，meta uuid 确定性）；④ CLI headless 构建 `--build "taskName=wechatgame;platform=wechatgame;..."` 产出完整小游戏（game.js/game.json/project.config.json/assets/main/index.js 266KB 含布防×7/夜战×15/物资雨×3/BLOOD_MOON×11/runSimulation×5）；⑤ project.config.json 写入 M2 同款 appid wx6be7146eb0d60752；⑥ **门⑥复跑 `cli open --project …/build/wechatgame` → `✔ open` exit=0**。附带修复：移植时发现浏览器 entry 的 DUSK→NIGHT off-by-one（取 sessions[idx] 应为 sessions[idx+1]），已修 |
| P7 | 可用性自测清单 + 台账 | 本提交 | §3 自测清单 + 本档 |
| 附 | FPS 采样预热剔除 | `0ca4a39` | budgetSamples（预热后）/fpsSamples（原始透明保留）拆分；加载毛刺不再误判预算 |

## 2. 整体验收门状态

| 门 | 状态 | 说明 |
|---|---|---|
| ① 渲染零硬编码色值 | ✅ | check-theme 全绿：白盒源码+index.html+bundle 扫描，色板 12 色之外 hex 即 FAIL；裸 rgba(数字) FAIL；bundle 与源码同步守卫（重编译逐字节比对） |
| ② 一天循环全相可演示 | ✅ | DAWN_SETTLE→DAY→DUSK_FORECAST→NIGHT 四相 UI 状态机（state.ts 单测锁定单向循环）；交互链：▶夜确认→入夜→布防确认→全屏夜战→天亮了→结算→继续→下一日循环；四相 headless 截图全部正常渲染 |
| ③ 动效表五条曲线 100% 落地 | ✅ | theme.test 逐条比对：fast 150 easeOutQuad（卡滑入）/normal 300 easeOutCubic（模态/翻面/波次充能）/rain 500 easeOutBack（物资雨/飞图标）/threat 300 easeInQuad repeat2（血月红闪+震屏）/dissolve 800 linear（昼夜转场），缓动函数参考值逐点断言；另 counter/stagger 两枚 tokens 承载 §3.4 明确值（800 easeOutCubic / 60ms） |
| ④ 帧率预算 min≥50fps | ✅ | 真实浏览器（macOS Chrome，750×1624 画布）：FPS 120（min 120 / avg 120）预算达标；2 窗预热剔除加载毛刺（原始样本透明保留，`0ca4a39`） |
| ⑤ 热区/字号合规 | ✅ | check-ui-compliance 全绿：交互热区 14 处 ≥88×88（dock×4/设置/夜战技能×2/夜战返回/DUSK 布防/结算继续/模态关闭/确认/事件选项/页返回）；字号零字面量（全 T.typography）；body 24 / caption 18 下限锁定 |
| ⑥ 微信构建回归（Creator 接入后） | ✅ | Creator 3.8.8 headless 构建的微信小游戏（`creator/build/wechatgame/`，appid=M2 同款）在微信开发者工具复跑：`cli open --project` → `✔ open` exit=0；构建含白盒三屏完整代码（见 P6 行证据）；CI 不含引擎构建（Creator 为 macOS 本机二进制），门⑥按目标路径在本机复跑 |
| ⑦ 独立提交 + 台账 | ✅ | §1 表：P1–P5+修正每点独立提交并记录哈希；本档为台账 |

## 3. 可用性自测清单（一天循环全相走查）

| # | 走查项 | 规则（UI 规范） | 验证方式与结果 |
|---|---|---|---|
| 1 | DAWN 结算三段式 | §3.4 雨 500ms→计数 800ms→逐户 60ms | anim.test 时序断言（counter 中途值过半/满值、popProgress 窗口、settleDoneAt 总长）+ DAWN 相截图（+320 滚动中帧） |
| 2 | 事件卡插播与队列 | §3.1 队列最多 2，先进后出恢复 | state-layout.test：第 3 张拒绝/恢复序断言 ✅ |
| 3 | 事件卡打断常规模态 | §3.1 任意模态可被打断 | 单测：panel 开→事件卡置顶→关闭后恢复 panel ✅；真机页面事件卡浮层截图 ✅ |
| 4 | 夜战不可打断 | §四 全屏接管 | 单测：NIGHT 相 pushEvent/openModal 均拒绝 ✅；夜战面板全屏截图 ✅ |
| 5 | 选择即锁定 | §3.2 不可回退 | 单测：锁定后再选为无操作 ✅；渲染层选项按钮点击后消失、翻面出结果 |
| 6 | 入夜转场 | §3.1 dissolve 800ms + 色板切 bg_night | dissolveAlpha 单测（0/0.5/1）+ NIGHT 相 bg_night 截图 ✅ |
| 7 | 血月入场演出 | §3.3 红闪×2+震屏 | threatBurst 单测（双闪窗口/窗外归零/震幅>0）+ 血月夜战截图（血红标记）✅ |
| 8 | 路血条三态 | §3.3 红/警/绿 | routeView 单测（0.9/0.97/1.2 三档）+ 夜战截图（绿 102% 与未揭示 ？？ 并存）✅ |
| 9 | 静默夜预告遮罩 | §四 SILENT 替换「?」 | 渲染分支 + DUSK 横幅实现（截图为血月路径；静默路径同分支）✅ |
| 10 | dock/技能/继续等热区 | §一.3 ≥88×88 | check-ui-compliance 14 处断言 + hitTest 单测 ✅ |
| 11 | 图鉴/商店/设置可达 | §3.5 | ⚙→设置→图鉴/商店→返回链路（hitTest nav 分支单测）+ 三页截图 ✅ |
| 12 | fail-safe 存档点提示 | §四 三检查点 | 设置页常驻文案「存档三检查点：日间/黄昏/夜战」（占位级；真实 fail-safe 恢复为 M3 运行时功能）|

## 4. FINDING（M2.5 发现，M3 输入）

- **FINDING-M2.5-1（事件卡 replay 语义）**：白盒事件卡是模拟回放——选项点击只驱动翻面动画，不改变已跑完的模拟（且 rollOutcome 现只掷 options[0]）。M3 实装交互事件时需把「选择→rollOutcome→applyEffects」接入运行时循环，§3.2 多选项才是真选择。
- **FINDING-M2.5-2（警戒带缺黄色 token）**：路血条三态的中档（0.95≤r<1）以 gold_deep 暗金承担，与「gold 只用于收租/货币」的色彩角色约束存在借用。建议 M3 在 theme.json 增补 `warn` 色 token 并同步修订 UI 规范 §二 色板表（走 ADR/文档修订）。
- **FINDING-M2.5-3（motion tokens 扩充）**：counter（800 easeOutCubic）/stagger（60ms）为落地 §3.4 而新增，§二 motion 表五条原样未动；追溯矩阵需登记该两枚 tokens 与 §3.4 的映射。
- **FINDING-M2.5-4（Creator 移植面，已解决）**：渲染层纯 TS 拆分使 Creator 移植 = 两个薄桥（离屏画布 + 动态纹理/输入）。本轮以 headless 路线完成（手写 3.8 序列化场景 + 确定性 meta + CLI `--build "key=value"`），GUI 非必需；Creator 窗口为 GPU surface（CUA 不可投影）时 headless 路线是唯一可自动化路径。运行时逐帧 `Texture2D.uploadData` 为占位实现，M3 换原生 2D 节点渲染（路线 B）时移除。
- **FINDING-M2.5-5（资产管线可换芯）**：管线 Stage1（候选来源）当前为确定性程序化生成；M3 换 AI 生成时仅替换该段，挑选/清理/校验/图集集成段直接复用（check-assets.mjs 即 §十六 清单的机器化实现）。

## 5. 验证命令（可复跑）

```bash
npm ci
npm run typecheck          # 主工程 + whitebox strict 双项目
npm test                   # 54 tests（逻辑 38 + 白盒渲染数据 16）
node scripts/check-config.mjs      # 六张表
npm run check:theme        # 门①：渲染层零硬编码色值 + bundle 同步守卫
npm run check:ui           # 门⑤：热区 ≥88px / 字号 tokens / motion 引用
npm run check:assets       # §十六：17 资产（色板±10%/72px 剪影/描边/透明底/命名）
npm run verify             # ALL GREEN（V0–V12，M2 硬门不回退）
npm run sync:cocos:check   # Cocos 共享产物无漂移
npm run build:whitebox && open apps/client-cocos/whitebox/index.html
# 交互演示：点击 dock/⚙/事件入口；?phase=dusk|night|dawn 直达相；?page=codex|shop|settings 直达页
# 帧率：页面顶部实时采样 + 控制台 __fpsReport()（预算 min ≥50fps）
"/Applications/wechatwebdevtools.app/Contents/MacOS/cli" open --project "$(pwd)/apps/client-cocos/minigame"   # ✔ open exit=0

# P6 Creator 工程（需本机 /Applications/CocosCreator.app 3.8.8）：
npm run sync:creator       # packages/sim/whitebox/tables → creator/assets/scripts（14 文件 + creator-sync.json 校验和）
npm run gen:creator-scene  # 确定性 meta + main.scene（Creator 3.8 序列化）
"/Applications/CocosCreator.app/Contents/MacOS/CocosCreator" --project "$(pwd)/apps/client-cocos/creator" \
  --build "taskName=wechatgame;platform=wechatgame;outputName=wechatgame;debug=true;buildPath=project://build;startScene=78ec8124-bb19-49f0-a482-9dbc536fa48c"
"/Applications/wechatwebdevtools.app/Contents/MacOS/cli" open --project "$(pwd)/apps/client-cocos/creator/build/wechatgame"  # 门⑥：✔ open exit=0
```

## 6. 逻辑零变化证明（sim.ts 加法投影）

- 变更：`runSimulation` 返回值新增 `eventCards`（渲染元数据投影），records/finalHash 未触碰。
- 证据：变更前后 `npm run simulate -- --days 7` 均 `finalHash=0bd81871`（累计死亡=1、教学事件=19 一致）；`npm run verify` ALL GREEN（V0–V12 含 β_sim/ΣI 硬门）全绿。
