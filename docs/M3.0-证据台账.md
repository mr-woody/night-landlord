# M3.0 证据台账（m3p0/world-design 分支：世界空间重构——文档/配置/逻辑/渲染）

> 契约源：docs/世界观与空间结构设计.md v1.0（上位）+ docs/可执行目标-M2.5-UI风格化.md 的里程碑纪律（一个功能点：实施→自检→验证→提交）。
> 红线核查：M0 数值契约（verify ALL GREEN）、applyEffects 单点（D4）、确定性 RNG、插件化——全程未破。

## 1. 功能点提交台账

| 功能点 | 内容 | 提交 | 证据 |
|---|---|---|---|
| ① | 世界观与空间结构设计 v1.0 定稿（三层空间模型/类 3D 视觉/插件清单/冲击矩阵/Q1–Q6 决议） | `8df4b67` | 用户授权按建议项推进；下游修订矩阵 §8 |
| F1b | 架构需求文档 v2.0：FR-W1–W5（worldmap/explore/interior/wildlife/渲染 NFR） | `1c51c60` | FR 全部落在插件化框架（FR-A/D/E/F 延续） |
| F2 | 数据配置表 v2.0：四张世界表 schema + EXPLORE_* 常量段 + 校验断言 | `dc9847f` | 文档 §9 |
| F3 | UI/UX v2.0 三层视图规范 + 美术 v3.0 类 3D 资产规格（B6–B9 批次） | `f2fe771` | 文档 §七/§十八 |
| F4 | 四张表 JSON 落地 + check-config 扩展 | `2bfefe5` | map_def 17/explore_def 4/gather_table 12/wildlife 4 全过；自检抓到深林 gatherSlots 数据不一致并修复 |
| F5 | `@rn/world` 逻辑插件（世界状态/探索引擎/野物遭遇，EffectOp 单点） | `86b9ab2` | 单测 9 项：确定性双跑 hash 全等/applyEffects 增量=totalYield/护栏禁 KILL/体力耗尽拒绝/采集冷却/D30 解锁/序列化往返 |
| F6 | L2 等距小区地图 + L3 房屋内部视图 | `c54a06a` | 等距楼栋 6 层体积+窗灯映射+锁定标注；室内工事位×2/住户立绘/门窗；headless 截图双视角验收 |
| F7 | L1 野外大地图 + 探索交互 | `7a5ccbb` | 四区域卡+详情+派出（@rn/world 真实 dispatchParty）；归来战报浮层；截图验收 |
| F8 | 夜战楼栋×房间维度投影 + 事件库世界组 10 条 | 本提交 | 夜战路条/日志显示 `A栋 n 层 m 号`（F 编号→栋 A 投影，M0 窗口不变名）；evt_wd×6+evt_in×4 入库（check-config 组断言）；M3.0 台账（本档）+ 追溯矩阵 §9 登记 |

## 2. 验收门状态

| 门 | 状态 | 证据 |
|---|---|---|
| M0 数值契约不破 | ✅ | verify ALL GREEN（V0–V12） |
| 四表 schema + 交叉引用 | ✅ | check-config 世界空间四表行（17/4/12/4） |
| 逻辑确定性 | ✅ | world.test 确定性双跑 hash 全等；40 seeds 遭遇率断言 |
| 护栏（D4/禁 KILL） | ✅ | 产出全部 EffectOp；野物战败仅 WOUND（单测：住户数量不减） |
| 渲染红线（tokens 零硬编码） | ✅ | check-theme 全绿（M2.6 视觉升级代码同步适用） |
| 热区/字号红线 | ✅ | check-ui 全绿 |
| 三层视图可演示 | ✅ | headless 截图：L2 等距小区/L3 室内/L1 野外 三相 |

## 3. FINDING（M3.0 → 后续里程碑）

- **F-1 探索中途事件触发**：evt_wd_* 目前经日事件管线（dayMin 门控）触发；探索途中触发需 director 集成（explore 插件产出事件候送 selectDay），排 M3.3。
- **F-2 野外产出与主经济咬合**：探索产出入独立副 GameState；与主存档经济合并（同资源池、检查点覆盖）排 M3.3，合并时跑 V13 产出锚点（EXPLORE_YIELD_TARGET_*）。
- **F-3 B/C 栋解锁内容**：D30 解锁仅有楼体；楼内房间/住户扩容内容排 M3.4。
- **F-4 类 3D 深度**：等距为程序化 2.5D；真 3D 光影/Spine 立绘待素材管线（M3 素材期）。

## 4. 验证命令（可复跑）

```bash
npm run typecheck && npm test            # 71 tests（63+9 world，实际 63 含 world 9）
node scripts/check-config.mjs            # 六表 + 世界空间四表
npm run verify                           # ALL GREEN
npm run build:whitebox && open apps/client-cocos/whitebox/index.html
# ?page=map（L2 小区）→ 点 A 栋 → 点房间（L3 室内）；?page=wild（L1 野外）→ 选区域 → 派出
```


---

# M3.2 战斗演出与天气系统（续册，2026-08-31）

> 契约源：docs/战斗演出与天气系统设计.md v1.0。演出/结算分离红线：runNight 结算不变，battleDirector 表现层回放。

| 功能点 | 内容 | 提交 | 证据 |
|---|---|---|---|
| F1 | 设计文档 v1.0（K-H1 融合决议/K-W1 天气数值暂缓 ADR/K-B1 自动为主） | `4309e7a` | 文档 §1–§5+冲击矩阵 |
| F2 | weather.json（6 天气×光照/粒子/系数/分段权重）+ building_def 房屋 6 级（durability 0.8→1.5）+ check-config 断言 | `fdf136c` | check-config 三行统计 |
| F3 | @rn/weather：weatherOfDay 确定性序列（血月强制尘暴/D8 权重分段）+ resolveDue 天气系数传导 | `1cf8c43` | 单测 4 项：双跑/血月跨 seed/权重分段/系数传导（0.5 采集减半、2× 遭遇） |
| F4 | 渲染天气层：光照 overlay+雨丝 130/雪花 90/雾带/血月尘+HUD 角标 | `494ee32` | headless 截图 D1 阴/D17 雨/D29 雪 |
| F5 | 独栋小屋群落：30 栋错排 6 级进化外观+金门/窗光/烟囱/新开放角标 | `cdc1c28` | headless 截图 D30 全群落 |
| F6 | 夜战演出编排：battleTimeline（每波 advance/attack/result）+ monsterProgress + 差异化怪物绘制（爬行/携梯/挂钩/悬停/精英）+ 守卫红臂章/崩解粒子 | `4378da1` | 单测 3 项（时间线确定性/排序/插值单调）；headless 截图血月尘暴夜全流程 |
| F7 | 房屋升级交互：点击小屋→面板（等级/消耗）→EffectOp 消耗→等级覆盖渲染 | `a265793` | headless 截图小屋 6 号 Lv0→Lv1 面板（建材×50） |

## 验收门（M3.2）

| 门 | 状态 | 证据 |
|---|---|---|
| 演出/结算分离 | ✅ | battleTimeline 只读 session；单测确定性复现；runNight 零改动 |
| M0 契约 | ✅ | verify ALL GREEN 全程 |
| tokens 红线 | ✅ | check-theme 全绿（天气色调引用 theme 键） |
| 房屋进化可视 | ✅ | D1 vs D30 截图对比（Lv0 茅草 → Lv5 砖石） |
| 天气可视 | ✅ | D1 阴/D17 雨/D29 雪 三态截图 |

## FINDING（M3.2 → 后续）

- **F-5 房屋耐久接入夜战**：house.durability 系数已入表，接入 runNight 路 HP 需 ADR（M0 窗口内房间恒 Lv1+，锚点不破）。
- **F-6 天气对夜战数值影响**（雨威胁+5%/尘暴+10%）同上，ADR 评估后排期。
- **F-7 探索中途事件触发**（M3.0 F-1 延续）。
